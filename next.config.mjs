/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.pixabay.com",
      "supabase.co",
      "img.youtube.com",
      "youtube.com",
    ],
  },
};

export default nextConfig;
